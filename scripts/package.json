{
  "name": "scripts",
  "version": "0.0.0",
  "license": "MIT",
  "bin": "./build/cli.js",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "dev": "pastel dev",
    "build": "pastel build",
    "prepare": "pastel build",
    "postinstall": "npx typesync",
    "serve": "node dist/index.js",
    "start": "ncc run src/index.ts --quiet"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "ink": "^3.0.8",
    "pastel": "^1.1.1",
    "react": "^17.0.1"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@vercel/ncc": "^0.27.0",
    "postinstall-postinstall": "^2.1.0",
    "typescript": "^4.1.3",
    "typesync": "^0.8.0"
  }
}
